{
  "name": "shopiflow",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "setup": "./setup.sh",
    "dev": "npm --workspace=apps/frontend run dev",
    "build": "npm --workspace=apps/frontend run build",
    "start": "npm --workspace=apps/frontend run start",
    "lint": "npm --workspace=apps/frontend run lint",
    "test": "npx tsx scripts/reset-test-db.ts && vitest --config vitest.config.ts",
    "test:run": "npx tsx scripts/reset-test-db.ts && vitest run --config vitest.config.ts",
    "test:unit": "vitest run --config vitest.config.ts tests/unit",
    "test:integration": "npx tsx scripts/reset-test-db.ts && vitest run --config vitest.config.ts tests/integration",
    "test:ui": "vitest --ui --config vitest.config.ts",
    "test:coverage": "vitest run --coverage --config vitest.config.ts"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "@vitest/ui": "^1.6.0",
    "concurrently": "^8.2.0",
    "dotenv": "^16.4.5",
    "global-agent": "^4.1.3",
    "pg": "^8.12.0",
    "socks": "^2.8.10",
    "socks-proxy-agent": "^10.1.0",
    "vitest": "^1.6.0"
  }
}
